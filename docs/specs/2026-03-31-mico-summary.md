#### 一、基本信息 (Frontmatter)

**名称 (name)**
- 使用小写字母、数字、连字符，长度 ≤64 字符
- 动词开头，描述动作而非对象
- 示例：`pdf-processor`（非 `pdf`）、`gh-create-issue`（非 `github-tool`）

```yaml
name: mico-work-summary
```

**描述 (description)**

这是决定 Skill 何时被触发的**唯一依据**。AI 始终加载此字段，触发后才读取正文。

回答以下问题来撰写描述：
1. **功能边界**：这个 Skill 支持哪些具体功能？（列举 2-3 个核心场景）
2. **触发词汇**：用户会用什么词汇来描述这些需求？（记录典型表达）
3. **边界说明**：哪些相似但不支持的功能需要明确排除？

```yaml
description: |
  将原始工作记录（自然语言描述）转换为结构化工作报告，输出至 ~/mico-work-summary/YYYY/MM/YYYY-MM-DD.md。
  支持周报(weekly)、月报(monthly)、年报(yearly)三种时间跨度，周报节点默认为每周五。
  年报模式下同时生成两份报告：年度重点工作总结（面向协同方）+ 员工自评（含敏感数据与价值观复盘，面向评审方）。
  当用户提及"周报"、"月报"、"总结"、"年终自评"或要求整理工作记录时触发。
  不支持：项目计划制定、OKR 目标设定、非工作相关的个人日记。
```

> **反模式警告**：不要在 description 中使用尖括号 `<>`，字符数 ≤1024。

---

#### 二、功能边界与触发场景

用具体例子建立共识，避免抽象描述。

**具体例子收集**（至少 3 个）

对每个例子，填写：
- **用户请求**：_"帮我..."_
- **预期输出**：_"生成/提取/转换..."_
- **不支持场景**：_"不包括..."_

| 例子 | 用户请求 | 预期输出 | 不支持场景 |
| ---- | -------- | -------- | ---------- |
| 1 | "帮我整理这周周报：MicoWorld做了直播间礼物动效"（首次使用MicoWorld） | 生成 `~/mico-work-summary/2025/03/2025-03-21.md`，含业务进展、风险问题、解决方案、下一步计划四章节；首次使用时需确认业务背景（MicoWorld=出海社交视频直播App）和主题选项 | 不包含：代码行数统计、需求/技术细节罗列 |
| 2 | "把3月份的几周周报汇总成月报" | 生成 `~/mico-work-summary/2025/03/2025-03.md`，基于该月所有周报素材和用户提供的月度趋势输入，提炼业务里程碑与主要风险 | 不包含：自动生成下月计划 |
| 3 | "帮我整理这周周报：MicoWorld做了PK连麦功能"（已有MicoWorld知识库） | 生成 `~/mico-work-summary/2025/03/2025-03-28.md`，复用已有业务背景，自动更新知识库历史主题 | 不包含：删除或覆盖原有业务总结内容 |
| 4 | "生成年终报告" | 同时生成两份报告：(1) `~/mico-work-summary/2025/2025-12-31-summary.md`（年度重点工作总结-事实导向），(2) `~/mico-work-summary/2025/2025-12-31-self.md`（员工自评-评价导向） | 不包含：薪资建议、职级晋升申请 |

**边界反模式识别**

标记以下场景：
- [x] 功能过于宽泛（如"处理所有文档格式"）→ 已限定为工作汇报场景，排除日记/计划类
- [ ] 用户请求模糊（如"优化代码"）
- [ ] 输出不确定（如"自动选择最佳方案"）

已排除场景：非工作时间记录、个人学习笔记、项目提案文档、OKR 制定。

---

#### 三、核心工作流 (SKILL.md Body)

**工作流类型选择**

选择最契合任务特性的组织方式：
- [x] **决策树**（条件分支）：根据时间跨度参数(weekly/monthly/yearly)和节点状态(新建/迭代)选择不同路径

**输入约定**

- **工作记录**：自然语言描述，口语化输入即可，如"周一修了几个bug，周二开会讨论需求"
- **时间信息**：可选，支持"本周"、"上周五"、"3月24日"等表达方式，周报默认每周五为节点
- **月报/年报额外输入**：跨周期汇总时，用户需提供额外的趋势性描述、里程碑信息或特别强调事项
- **业务域信息**：用户输入中提及的业务名称（如"MicoWorld"、"SoulStar"），skill自动检查知识库，首次使用需用户简要说明业务背景，后续直接读取

**工作流草稿**

使用祈使句描述步骤，每个步骤 ≤2 行：

```markdown
## 解析输入与时间定位
解析用户输入的时间信息（本周/本月/本年/具体日期），周报默认节点为每周五
接收自然语言描述的工作记录作为输入
生成目标文件路径（基准目录 ~/mico-work-summary/）：
  - 周报：YYYY/MM/YYYY-MM-DD.md
  - 月报：YYYY/MM/YYYY-MM.md
  - 年报（双文件）：YYYY/YYYY-12-31-summary.md + YYYY/YYYY-12-31-self.md

## 判断节点状态
检查目标文件路径是否已存在
若不存在：执行【新建报告流程】
若已存在：执行【迭代更新流程】

## 新建报告流程（周/月/年报）
读取 references/report-structure.md 中的结构模板
提取业务域（如"MicoWorld"），检查 ~/mico-work-summary/.knowledge/business-knowledge.md 是否存在该业务：
- 若不存在：询问用户业务背景（类型/核心特性），创建知识库条目
- 若存在：读取业务特性及历史主题记录
基于业务特性 + 用户当前输入，推测主题方向，提供选项供用户选择或确认
用户确认后生成业务进展总结（必填），风险与问题、解决方案、下一步计划按需生成（用户未提及则简单询问一句，无则跳过，不臆造）
生成后自动更新知识库（记录本次主题到历史记录，发现新维度时更新）
写入目标文件路径，追加迭代标记（记录最后修改时间）

## 迭代更新流程
读取现有报告文件内容
识别用户输入中的新增/变更事项
合并到对应章节，保持原有业务逻辑连贯性
更新迭代标记（更新最后修改时间）

## 跨周期汇总（月报/年报特有）
扫描对应周期内所有子报告文件（如月报读取该月所有周报）作为参考素材
请求用户提供额外输入：跨周度的业务趋势、月度里程碑、需要特别强调的成果或风险
结合周报素材和用户补充输入，生成高层级总结，不重复罗列子报告细节

## 年终双报告流程（yearly特有）
当 period=yearly 时，同时触发两个生成流程：
- 年度重点工作总结：聚焦"做成了什么"，基于工作事实的重点产出整理（面向协同方）
- 员工自评：聚焦"做得怎么样"，基于岗位预期的自我评价与复盘（面向评审方）
两份报告分别输出至同一目录：yyyy/12/yyyy-12-31-summary.md 和 yyyy/12/yyyy-12-31-self.md

## 年度工作总结撰写要点
加载 references/yearly-summary-guide.md
- 聚焦最重要的几项工作产出，拒绝事无巨细的todo罗列
- 颗粒度适中：抓住关键举措 + 关键结果，提供具体业务数据
- 明确项目角色、进度、结果，拒绝假大空和拿来主义
- 不包含敏感数据/关键业务信息（此类内容放入自评的"核心业绩"）

## 年终自评撰写流程
加载 references/self-evaluation-guide.md 第一部分，理解双文档关系：
- 核心业绩：可补充敏感数据/关键业务信息（不方便在年度总结中公开的内容）
- 或输出全年工作思考：战略打法、关键举措、收获沉淀（与年度总结的事实陈述形成互补）
- 个人复盘：亮点与待提升点，基于岗位预期的自我评估

自评填写要点（加载 references/self-evaluation-guide.md 第二部分）：
- 对比预期：通过具体事例、行业数据对比，说明结果与预期差异
- 未来发展：针对需要提升或持续保持的部分，进行思考沉淀
- 成功经验：回顾做得好的部分，提炼可复制的方法论
- 根因分析：未达成部分深挖主观原因和客观原因，明确后续改善方向

撰写价值观部分时（可选），加载 references/self-evaluation-guide.md 价值观章节
- 引导用户基于四个价值观维度（正念/极端/简单/谦卑）选择最相关的1-2个
- 结合具体行为案例撰写，避免空泛表态

撰写管理自评部分时（可选，仅Leader），加载 references/self-evaluation-guide.md 管理维度章节
- 基于三个管理维度（管好业务/带好团队/传好文化）进行自我评估
- 结合团队管理实例说明管理实践和改进方向
```

> **简洁检查**：如果 SKILL.md 正文超过 500 行，考虑拆分内容到 references/。

---

#### 四、可复用资源规划

对每个具体例子，问自己：
1. 哪些操作会被反复执行且易错？ → 候选 **scripts/**
2. 哪些知识需要查阅但不常变化？ → 候选 **references/**
3. 哪些模板可以直接复用？ → 候选 **assets/**

**Scripts 规划**

| 脚本名 | 功能 | 输入参数 | 输出 | 确定性 (1-10) |
| ------ | ---- | -------- | ---- | ------------- |
| calc-report-path.sh | 根据日期和周期类型计算报告文件路径 | date(YYYY-MM-DD), period(weekly/monthly/yearly) | 绝对路径字符串 | 10 |
| extract-week-range.sh | 计算指定日期所在周的起止日期和ISO周号 | date(YYYY-MM-DD) | 起始日期、结束日期、ISO周号 | 10 |
| scan-period-reports.sh | 扫描指定周期内的所有子报告文件 | start_date, end_date, base_dir | 文件路径列表 | 10 |

> **脚本判断标准**：确定性 ≥8 时封装成脚本（即操作 100% 可复现）。
> 路径计算逻辑需精确，避免手动计算错误。

**References 规划**

| 文件名 | 内容类型             | 何时加载         | 长度预估 |
| ------ | -------------------- | ---------------- | -------- |
| report-structure.md | 报告结构模板与写作指南 | 每次生成报告前 | ~80行 |
| classification-guide.md | 工作主题提炼方法论（分层理解→动作+对象→多维验证） | 提炼工作主题时 | ~120行 |
| yearly-summary-guide.md | 年度工作总结撰写指南（聚焦/有效/完整/拒绝） | 撰写年度工作总结时 | ~60行 |
| self-evaluation-guide.md | 自评填写方法论（核心业绩/个人复盘/价值观/管理自评） | 撰写员工自评时 | ~150行 |
| values-framework.md | 价值观维度参考（正念/极端/简单/谦卑释义与行为准则） | 撰写价值观自评时 | ~70行 |
| leadership-framework.md | 管理维度参考（管好业务/带好团队/传好文化释义） | Leader撰写管理自评时 | ~90行 |

> **拆分时机**：单个 reference ≥100 行时添加目录；SKILL.md 引用时说明"何时需要查阅此文件"。
> report-structure.md 包含：四个标准章节定义、每章节的写作要点、反模式示例（禁止平铺罗列）、信息不足时引导用户补充的策略、工作主题抽象方法论（提供选项+解释让用户选择）。

**业务知识库 (business-knowledge.md)**

这是**运行时动态数据**，不是静态参考文档，单独说明：

| 属性 | 说明 |
|------|------|
| **存储路径** | `~/mico-work-summary/.knowledge/business-knowledge.md` |
| **数据格式** | 每业务域一个条目：`## 业务名称` 下含 `类型`、`核心特性`、`常见主题列表` |
| **读取时机** | 每次提取业务域后（判断是否存在该业务） |
| **写入时机** | 新建报告完成后（追加本次主题到历史记录） |
| **并发安全** | 单用户单会话写入，无需加锁；追加模式写入，禁止覆盖 |

示例条目格式：
```yaml
## MicoWorld
类型: 出海社交视频直播App
核心特性: [实时互动, 虚拟礼物, 主播PK]
历史主题:
  2025-03: [商业化变现-礼物动效, 社交互动功能-PK连麦]
```

**Assets 规划**

| 资源名 | 类型            | 使用场景                 |
| ------ | --------------- | ------------------------ |
| weekly-template.md | 周报模板 | 新建周报时作为内容骨架 |
| monthly-template.md | 月报模板 | 新建月报时作为内容骨架 |
| yearly-template.md | 年报模板 | 新建年报时作为内容骨架 |
| yearly-self-template.md | 年终员工自评模板 | 新建年终自评时作为内容骨架 |

---

#### 五、自由度分配

为每个步骤/功能标注约束强度，确保匹配任务特性。

| 步骤/功能 | 自由度   | 约束形式             | 理由                   |
| --------- | -------- | -------------------- | ---------------------- |
| 路径计算与文件定位 | 低 | calc-report-path.sh 脚本 | 日期计算逻辑复杂易错，必须精确 |
| 报告结构组织 | 中 | references/report-structure.md 模板 | 有标准四章节框架，但需根据业务灵活填充 |
| 工作事项分类提炼 | 高 | 文本指令 | 业务上下文差异大，需AI判断归类 |
| 风险与解决方案撰写 | 高 | 文本指令 | 需基于经验提炼，无法模板化 |
| 跨周期汇总摘要 | 中 | 伪代码逻辑 | 有提炼模式但需根据内容调整 |
| 迭代更新流程 | 中 | 伪代码/文本指令 | 有合并模式（新增/变更/删除）但需根据内容判断具体类型 |
| 业务知识库管理 | 低 | 文本指令 + 严格数据格式 | 涉及持久化数据格式一致性，按固定格式读写，禁止覆盖 |
| 年终自评撰写（价值观/管理维度） | 高 | 文本指令 + self-evaluation-guide.md | 需结合个人实际案例，参考维度释义但不照搬 |

**三档位判断标准**：

- **高自由度（文本指令）**：多种做法皆有效，需上下文判断
  - 问：这个任务有 3 种以上正确做法吗？
  
- **中自由度（伪代码/参数化脚本）**：有推荐模式但允许调整
  - 问：存在"通常这样做"的模式，但需要根据情况微调吗？
  
- **低自由度（具体脚本）**：操作易错或必须严格一致
  - 问：错了会导致数据丢失/不可逆后果吗？

> **反模式警告**：避免全流程使用低自由度（过度刚性）或全流程高自由度（边界缺失）。

---

#### 六、质量检查清单

**Frontmatter 检查**

- [x] name 符合命名规范（hyphen-case，≤64 字符）：mico-work-summary
- [x] description 包含所有触发条件（≤1024 字符，无尖括号）
- [x] description 明确排除了易混淆的场景：排除日记/计划/OKR等

**简洁性检查**
- [x] 使用祈使句替代叙述句：工作流步骤已使用祈使句
- [x] 解释"为什么"替代罗列禁令（强指令 ≤20%）：在自由度分配中说明理由

**渐进式披露检查**
- [x] SKILL.md 正文 <500 行：预估 200-300 行
- [x] 长内容拆分到 references/ 并在 SKILL.md 中引用：report-structure.md 等

> **待创建 SKILL 时处理**：
> - [ ] 删除所有辅助文档（README.md、CHANGELOG.md 等）
> - [ ] references 文件 ≥100 行时添加目录TOC

**自由度检查**

- [x] 易错操作封装成了脚本：路径计算脚本化
- [x] 灵活任务使用了文本指令：事项分类、风险撰写等高自由度
- [x] 没有出现"MUST/ALWAYS/NEVER"堆砌：使用"请勿"等柔性表达